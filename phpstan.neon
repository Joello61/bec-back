includes:
    - vendor/phpstan/phpstan-symfony/extension.neon
    - vendor/phpstan/phpstan-symfony/rules.neon
    - vendor/phpstan/phpstan-doctrine/extension.neon
    - vendor/phpstan/phpstan-doctrine/rules.neon

parameters:
    level: 6
    # Chemin projet explicite (par defaut : sys_get_temp_dir(), non predictible/cachable
    # de facon fiable d'un run CI a l'autre) - condition documentee par PHPStan lui-meme
    # pour que resultCache.php survive entre executions via actions/cache (CI). "tmp/" et
    # non "var/" : gitignore separe, jamais mele au cache Symfony existant.
    tmpDir: tmp
    paths:
        - src
    symfony:
        containerXmlPath: var/cache/dev/App_KernelDevDebugContainer.xml
    doctrine:
        objectManagerLoader: tests/object-manager.php
    ignoreErrors:
        # Idiome Doctrine standard : un champ obligatoire (colonne NOT NULL) est
        # declare nullable en PHP avec une valeur par defaut null, toujours fixee
        # via un setter ou #[PrePersist] avant le flush() - jamais garanti par le
        # systeme de types, mais convention largement acceptee dans l'ecosysteme
        # Symfony/Doctrine plutot qu'un refactor de construction sur 18 entites.
        - identifier: doctrine.columnType
        - identifier: doctrine.associationType
