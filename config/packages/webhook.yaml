framework:
    webhook:
        routing:
            stripe:
                service: App\Webhook\StripeRequestParser
                secret: '%env(STRIPE_WEBHOOK_SECRET)%'
            notchpay:
                service: App\Webhook\NotchPayRequestParser
                secret: '%env(NOTCHPAY_WEBHOOK_SECRET)%'
